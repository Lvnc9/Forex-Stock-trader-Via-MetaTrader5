# raw/

Store untouched downloads here.

```
raw/
  mt5/<SYMBOL>/<TF>/...
  dukascopy/<SYMBOL>/...
```

Do not commit large tick dumps or CSVs if repo policy forbids bulk market data (see root `.gitignore`). Keep a local copy for audit.
